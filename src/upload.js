import react, {useState} from 'react';
const Upload () => {
   [isUploading](false)};
const handleUpload ()=>{
    [setisUploading](true);
}; 
return(
    <div>
    <button onCLick={handleUpload}
    {handleUploading ? 'Uploading Image'}> 
    </button>
    </div>

);
